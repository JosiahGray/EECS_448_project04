(must be using terminal or command prompt, in the GameCollection/src directory)

To run the program, type:
javac GameCollection.java
java GameCollection

or to avoid the cleanup of *.class files,
on Linux, type: bash run.sh
on Windows, type: run.bat

To play, select a game from the menu.  Multiple instances of each game may be opened at the same time.


To test the games:

Select the game you would like to test from the menu to the right.
